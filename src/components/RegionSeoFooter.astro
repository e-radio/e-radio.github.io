---
import { localeHelpers } from '../i18n/index';
const { t } = localeHelpers(Astro.url);

interface Props {
  heading: string;
  text: string;
}

const { heading, text } = Astro.props;
---

<section class="mt-5">
  <h2>{t(heading)}</h2>
  <p>{t(text)}</p>
</section>
